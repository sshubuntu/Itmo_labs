package Command;

public enum CommandType {
    HELP,
    INFO,
    SHOW,
    ADD,
    UPDATE,
    REMOVE_BY_ID,
    CLEAR,
    EXIT,
    EXECUTE_SCRIPT,
    REMOVE_FIRST,
    SORT,
    HISTORY,
    MAX_BY_NAME,
    FILTER_CONTAINS_NAME,
    PRINT_FIELD_ASCENDING_DISTANCE
}
